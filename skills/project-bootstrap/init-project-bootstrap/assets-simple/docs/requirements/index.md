# Requirements Index

## 编号规则

- 需求编号使用 `REQ-0001` 格式，按创建顺序递增。
- 需求文件放在 `docs/requirements/`，文件名使用 `REQ-0001-short-title.md`。
- 同一需求的澄清、设计、开发计划和验证记录写入同一个文件。

## 状态说明

- `Clarifying`：正在澄清需求。
- `Designed`：澄清和设计已完成，等待或正在制定计划。
- `Planned`：开发计划已完成，等待或正在实现。
- `In Progress`：正在实现。
- `Verifying`：实现完成，正在验证或等待用户验收。
- `Done`：用户已验收。

## 需求列表

| 编号 | 标题 | 状态 | 文件 |
| --- | --- | --- | --- |
