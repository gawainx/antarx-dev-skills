---
name: design-plan-doc-writer
description: 需求澄清完成后，用于产出仓库对齐的设计文档与分阶段开发计划。适用于用户明确要求撰写设计文档、开发计划或二者同时产出时。复用已确认的需求和设计，只产出用户要求的文档，并遵循项目已有文档模式。
---

# 设计与计划文档编写

仅在需求边界、约束与范围已稳定时撰写文档。

## 输入与产物

复用已明确的需求、设计和用户决策，不重复确认。用户只要求设计时只写设计；只要求计划时基于已有设计写计划；要求两者时先设计后计划。缺少会影响计划的设计决策时，先解决该决策，不为补齐流程强制新建设计文档。

## 工作流
1. 先确认需求澄清已完成；未完成则继续澄清。
2. 检查仓库文档约定、相关已有文档和代码。先识别项目采用分离文档还是单需求文件模式，沿用既有路径和结构。
3. 阅读历史文档时，先服务于当前新特性的目标、范围和成功标准；历史文档中的约束边界不得直接作为新特性设计或开发计划的约束边界，至少不能作为第一优先级。
4. 如果当前新特性与历史文档约束存在冲突，必须先请求用户确认取舍；禁止未经确认就基于历史约束写入设计文档或开发计划。
5. 根据功能领域推断文档分类。
6. 检查是否存在同前缀文档。
7. 若存在，先比较范围和内容。用户已明确要求更新的，直接更新对应内容；只有目标或替换范围存在歧义时才询问。
8. 按用户要求写设计、计划或两者。分离文档模式下，计划引用相关设计文档；单需求文件模式下，更新同一文件的对应章节并使用章节引用。
9. 严控范围，明确纳入范围与不纳入范围。

## 输出规则
- 语言跟随用户；默认简体中文。
- 文件命名优先遵循用户要求和仓库约定；无约定时使用简洁的功能名称，不含空格或日期。
- 如果项目使用需求编号管理（如 issue ID、需求 ID、PRD 条目编号等），文件名、标题、关联设计文档与计划条目必须沿用该需求编号；编号格式跟随项目既有规范。
- 项目已有文档模式优先。没有约定时，设计文档放在 `docs/design-docs/`，计划放在 `docs/exec-plans/active/`；单需求文件模式只更新已有需求文件，不额外创建分离目录。
- 分离计划在用户确认完成后移动到项目约定的归档目录，默认 `docs/exec-plans/completed/`；单需求文件按项目约定更新状态。
- 分离文档模式下，同一需求的设计与计划使用相同 basename；单需求文件模式使用同一文件的对应章节。
- 计划任务状态统一：`Designed | Coding | Testing | Finished`。
- 开发计划中的“阶段”术语统一使用英文 `Stage`；禁止使用 `Phase`。
- 计划仅覆盖本轮已确认范围，不得静默扩 scope。
- 历史需求、设计文档和执行计划只能作为背景证据；禁止把其中的“非范围”“暂不实现”“不引入依赖”等负向边界直接写成当前新特性的范围限制。

## 设计文档结构

以下结构在无项目模板时使用；单需求文件模式映射到对应章节。
1. 文件名作为标题
2. `## 核心功能（WHAT）`
3. `### 需求背景（WHY）`
4. `### 需求目标（GOAL）`
5. `### 范围边界`
6. `## 实现流程（HOW）`
7. `## i18n` （如果不涉及则略过）
8. `## 测试用例`

### 设计要求
- 说明当前架构与最终技术决策。
- 明确触点、状态归属、关键约束等信息；
- 关键约束必须来自当前需求确认、当前架构事实或用户明确决策；如果约束来自历史文档且会影响当前新特性范围，必须先获得用户确认。
- 如果涉及 UI 场景，需写清布局结构、工具栏归属、宽度策略、空态与稳定性策略。
- 如果涉及国际化，`i18n` 需包含 key、占位符、语言映射、回退策略、影响面。
- 测试分编译检查、手工检查、回归检查。

## 开发计划结构

以下结构在无项目模板时使用；单需求文件模式映射到对应章节。
1. 文件名作为标题
2. `## 关联设计文档`
3. `## Stage`
4. `### 任务`

### 计划任务模板
计划任务必须包含如下内容：

- `## Stage #N: [Stage 名]`
- `### 任务 #N: [任务名]`
- `**Status:** Designed | Coding | Testing | Finished`
- `**Files:** Create/Modify/Verify` 记录修改的文件
- 功能 / 实现说明 / 预期验证结果：每个任务记录函数、实现功能、预期结果

### 计划要求
- 至少 1 个 Stage，每个 Stage 至少 1 个任务。
- 每个任务必须可验证，建议 15-90 分钟颗粒度。
- 风险驱动验证：高风险变更要求更强自动化或迁移校验。
- 增加最终整体验证任务（构建/回归检查 + 计划状态回写）。
- 完成每个 Stage 后进行 1 次原子提交。

## 旧文档处理
- 无同前缀：直接创建。
- 有同前缀：核对是否属于当前需求；沿用已授权的更新范围，保留无关内容，仅对实质歧义询问。

## 参考
- 参考 `references/framework.md`。
- 参考 `assets/` 中样例文档风格。
